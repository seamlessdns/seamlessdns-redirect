# Seamless legacy-domain redirect

GitHub Pages site that forwards `seamlessdns.org` visitors to the primary Seamless website at `seamlessconnect.org`.

The redirect preserves paths, query strings, and URL fragments in browsers with JavaScript enabled. Explicit pages cover the homepage and `/sponsors/`; `404.html` handles other legacy paths.
